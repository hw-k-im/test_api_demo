spring:
  application:
    name: test_api_demo

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080